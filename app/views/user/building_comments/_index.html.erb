<h3>コメント数:<%= comments.count %></h3>
<% comments.each do |comment| %>
  <div class="row" style="margin: 10px">
    <div class="col-3" style="text-align: center;">
      <%= attachment_image_tag comment,:image,format: 'jpeg', fallback: "new_toilet_image.jpg",size: "180x130" %>
    </div>
    <div class="col-9">
    　ユーザー名：<%= link_to comment.user.name,user_path(current_user.id) %>
      <p>投稿コメント：<%= comment.comment %></p>
      <% if comment.user == current_user %>
      <%= link_to "削除",building_building_comment_path(comment.building,comment),method: :delete, remote: true ,class: "btn btn-danger" %>
      <% end %>
      <div class="create-date">
        <span><%= comment.created_at.strftime('%Y/%m/%d %H:%M:%S') %></span>
      </div>
    </div>
  </div>
<% end %>